<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title><%= title %> · Pandora's Box</title>
  <link rel="stylesheet" href="/styles.css"/>
  <link rel="icon"  type="image/png" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/public/favicon_io/apple-touch-icon.png">
    <link
  rel="stylesheet"
  href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
/>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">


</head>
<body>
  <header class="nav">
    <div class="nav-inner container">
      <a class="brand" href="/">
        <span class="brand-badge">
          <img src="logo.png" alt="Pandora’s Box Logo" style="width:34px; height:34px; border-radius:10px;">
        </span>
        Pandora’s Box
      </a>
      <nav class="row">
        <% if (user) { %>
          <form action="/logout" method="POST" class="row" style="margin:0">
            <button class="btn btn-ghost" type="submit"> <i class="fa-solid fa-right-from-bracket"></i> Logout</button>
          </form>
        <% } else { %>
          <a class="login-in" href="/login">Login</a>
          <a class="sign-up" href="/register">Register</a>
        <% } %>
      </nav>
    </div>
  </header>

  <main class="container">
    <% if (messages && messages.success && messages.success.length) { %>
      <% messages.success.forEach(m => { %>
        <div class="alert alert-success"><span>✓</span><span><%= m %></span></div>
      <% }) %>
    <% } %>
    <% if (messages && messages.error && messages.error.length) { %>
      <% messages.error.forEach(m => { %>
        <div class="alert alert-error"><span>!</span><span><%= m %></span></div>
      <% }) %>
    <% } %>

    <div class="grid">
      <section class="card">
        <%- body %>
      </section>
    </div>
  </main>

  <footer class="footer">
    <% if (user) { %>
  <%- include('partials/bottomnav') %>
<% } %>

    © <%= new Date().getFullYear() %> Pandora’s Box By ATAGWE ROGER — secure, simple storage.
  </footer>

  <script src="/script.js">
  </script>
</body>
</html>
